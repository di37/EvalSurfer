"""EvalSurfer MCP tool modules.

Importing each module runs its ``@mcp.tool()`` decorators, registering that
domain's tools on the shared :data:`evalsurfer.mcp.instance.mcp` server.
:mod:`evalsurfer.mcp.server` imports them all.
"""
