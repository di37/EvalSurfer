# `evalsurfer/metrics/dataset/` — eval golden dataset

Versioned cases for running evaluations (optional gold answer/label/score +
coverage tags), with contamination controls and v1↔v2 diffing.

**Naming:** distinct from Analysis [`GoldenSet`](../../analysis/diagnostics/golden_set/)
— that self-tests the deterministic framework layer; this holds *eval* cases.
