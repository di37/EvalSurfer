"""Deterministic measurement — operational metrics, reference quality metrics,
and the golden dataset the metrics run against.

Groups the ``operational``, ``quality``, and ``dataset`` subpackages. Import from
the subpackage directly, e.g. ``from evalsurfer.metrics.operational import
OperationalMetrics``.
"""
