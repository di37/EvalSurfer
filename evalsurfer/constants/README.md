# `evalsurfer/constants/` — shared rubric catalog

Package-wide constants for categories, criteria, scales, decisions, and SLO
keys. Listed next to Core in docs because Core scoring consumes them, but the
catalog defines Quality / Operational / Safety for **all** CIMAA layers — it is
not Core-owned product code.
