"""Result analysis — diagnostics over a produced report and judge calibration
(the "eval of the eval").

Groups the ``diagnostics`` and ``calibration`` subpackages. Import from the
subpackage directly, e.g. ``from evalsurfer.analysis.diagnostics import Explainer``.
"""
